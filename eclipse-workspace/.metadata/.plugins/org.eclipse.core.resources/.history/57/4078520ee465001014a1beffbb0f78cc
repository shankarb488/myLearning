package javademo;

public class StringBufferBuilder {

	public static void main(String[] args) {
		String s = "is Automation iss the key learn";
		String[] splittedString = s.split(" ");
		String max = splittedString[0];
		String min = splittedString[0];
		String maxs;
		String mins;
		
		for(String s1:splittedString) {
			if(s1.length()>=max.length()) {
				maxs=s1;
				System.out.println(maxs);
			if(mins.length()<min.length()) {
				mins=s1;
				System.out.println(maxs);	
				
			}
			
		}

	}

}
